Requerimientos:
-pyodbc
-pandas
-flask
-nzpy


pip install pyodbc --trusted-host pypi.org --trusted-host pypi.python.org --trusted-host files.pythonhosted.org

python3.11 -m pip install nzpy --trusted-host pypi.org --trusted-host pypi.python.org --trusted-host files.pythonhosted.org


-----------------------------
-crear el entorno virtual:
python -m venv venv

-activar el entorno virtual:
.\venv\Scripts\activate

-desactivar el entorno virtual:
deactivate

-Para listar las librerias:
python -m pip list

-Pasar las librerias a la carpeta de requerimientos:
python -m pip freeze > requirements.txt


python -m pip install pandas
